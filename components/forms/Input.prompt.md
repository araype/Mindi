Text field with soft rounded corners and a rose focus ring, used in onboarding and profile forms. Real `<label htmlFor>`, `aria-describedby` wires the helper/error text, `aria-invalid` on error.

```jsx
<Input label="Tu nombre" placeholder="Escribe aquí" />
<Input label="Correo" error helperText="Revisa el formato" />
```
