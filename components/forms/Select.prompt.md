Dropdown select for choosing among a short list, e.g. symptom frequency, age range. Native `<select>` — full keyboard support, screen-reader semantics, no custom combobox to maintain.

```jsx
<Select label="¿Con qué frecuencia?" options={['A diario', 'Algunas veces', 'Rara vez']} />
```
