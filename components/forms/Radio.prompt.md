Native radio button for single-choice questions. Group with a shared `name` so arrow-key navigation and grouping work.

```jsx
<Radio name="frecuencia" label="Sí, todos los días" checked={false} onChange={...} />
<Radio name="frecuencia" label="Algunas veces" checked={true} onChange={...} />
```
