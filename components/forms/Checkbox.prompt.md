Native checkbox (accent-colored) for consent and multi-select lists — real keyboard/focus/SR semantics, 44px tap target via the label.

```jsx
<Checkbox label="Acepto recibir recordatorios suaves" checked={true} />
```
