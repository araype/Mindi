Minimal line-icon set (1.75px stroke, 24×24, Lucide-style proportions) — a hand-built substitute since no icon font/sprite was provided. Swap for the real system (Lucide/Phosphor via CDN, or Mindi's own set) when available; document the swap in readme.md.

```jsx
<Icon name="message" size={20} />
<Icon name="info" label="Información" />
```
