Small dark hover/focus label for extra context on icons or truncated text. Shows on focus as well as hover (keyboard/touch reachable), wired via `aria-describedby` + `role="tooltip"`.

```jsx
<Tooltip label="Guardado automáticamente"><button><Icon name="info" label="Información" /></button></Tooltip>
```
