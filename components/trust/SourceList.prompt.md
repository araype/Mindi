List of citations backing a piece of content. Never invent medical sources — if none exist yet, label entries "Ejemplo de fuente" so nobody mistakes the demo for real backing.

```jsx
<SourceList sources={[{ label: 'Ejemplo de fuente 1' }, { label: 'Ejemplo de fuente 2', href: '#' }]} />
```
