Neutral sunken panel that groups ReviewedBy + SourceList + a "cuándo consultar a un profesional" line under one piece of guidance, so trust signals sit together instead of scattered.

```jsx
<TrustPanel>
  <ReviewedBy name="Nombre del profesional / especialidad" />
  <SourceList sources={[{ label: 'Ejemplo de fuente' }]} />
</TrustPanel>
```
